package nslbattlecodemaster.util;

import battlecode.common.*;

/**
 * Created by morris on 1/11/17.
 */
public class Util {

    public static void lockOn(int targetId, String type, MapLocation myLocation) {
        // move so that (targetPosition - selfPosition) = (maxShootingRange - (maxTargetMovement/turn))

        int target = targetId;
        int strideRadius = RobotType[type].valueOf(strideRadius);
        int distanceToTarget = targetLocation - myLocation
        while (true) {
            if



        }
    }



}
